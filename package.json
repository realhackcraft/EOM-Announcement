{
  "devDependencies": {
    "browserify": "^17.0.0",
    "stylelint": "^15.2.0",
    "stylelint-config-recommended": "^10.0.1",
    "tinyify": "^2.0.0"
  },
  "scripts": {
    "bundle": "browserify node/final.js -p tinyify | terser -cm --comments false > bundle.min.js  ",
    "test": "node -p tinyify node/fetcher.js && node node/processor.js"
  },
  "dependencies": {
    "sheetdb-node": "^0.0.12"
  }
}
